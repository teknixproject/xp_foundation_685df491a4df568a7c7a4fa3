import React from 'react';

import { PreviewUI } from '@/components/grid-systems/ClientWrapGridSystem';

type Props = object;

const PreviewPage: React.FC<Props> = ({}) => {
  return (
    <div>
      <PreviewUI />
    </div>
  );
};

export default PreviewPage;
