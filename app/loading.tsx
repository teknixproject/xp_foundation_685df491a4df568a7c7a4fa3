import React from 'react';

import LoadingPage from '@/components/grid-systems/loadingPage';

type Props = object;

const Loading: React.FC<Props> = ({}) => {
  return (
    <div>
      <LoadingPage />
    </div>
  );
};

export default Loading;
